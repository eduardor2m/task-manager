CREATE TABLE IF NOT EXISTS task (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, description TEXT NOT NULL, completed INTEGER NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL)